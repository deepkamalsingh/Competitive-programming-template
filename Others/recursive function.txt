#include<bits/stdc++.h>
using namespace std;
int main(){
	auto Fib = [&](int _i) -> int {
		auto recurse = [&](int i, const auto &self) -> int {
			if (i <= 1) return int(1);
			return self(i-1,self) + self(i-2,self);
		};
		return recurse(_i,recurse);
	};
  cout << Fib(40);
}